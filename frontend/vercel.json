{
  "installCommand": "rm -rf node_modules/.cache .cache && npm ci --legacy-peer-deps || npm install --legacy-peer-deps",
  "buildCommand": "CI=false GENERATE_SOURCEMAP=false npm run build:web",
  "outputDirectory": "build",
  "framework": null
}

