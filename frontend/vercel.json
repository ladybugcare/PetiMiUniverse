{
  "installCommand": "rm -rf node_modules node_modules/.cache .cache package-lock.json && npm cache clean --force && npm install --legacy-peer-deps",
  "buildCommand": "CI=false GENERATE_SOURCEMAP=false npm run build:web",
  "outputDirectory": "build",
  "framework": null,
  "env": {
    "NODE_ENV": "production"
  }
}

