# Copy to .env.local in this folder (frontend/) and fill with your Supabase project.
# Dashboard: https://app.supabase.com → Project → Settings → API
# Restart `npm run web` after changing env files.

REACT_APP_SUPABASE_URL=https://YOUR_PROJECT_REF.supabase.co
REACT_APP_SUPABASE_ANON_KEY=your_anon_public_key

REACT_APP_API_URL=http://localhost:3000
# Produção (Vercel): após validar a API na Railway — depois cutover para:
# REACT_APP_API_URL=https://api.petmi.app

# PetMi Hub (app web separada, ex. Vite na porta 3002). Quando definido, links /hub/* no menu abrem esta URL.
REACT_APP_HUB_WEB_URL=http://localhost:3002
# Produção: REACT_APP_HUB_WEB_URL=https://hub.petmi.app

# Optional: same values for Expo / shared code paths
EXPO_PUBLIC_SUPABASE_URL=https://YOUR_PROJECT_REF.supabase.co
EXPO_PUBLIC_SUPABASE_ANON_KEY=your_anon_public_key
EXPO_PUBLIC_API_URL=http://localhost:3000
# Produção: EXPO_PUBLIC_API_URL=https://api.petmi.app
