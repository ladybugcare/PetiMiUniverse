import React from 'react';
import { HubDesignSystemShowcase } from '@petimi/hub-ui';

/** Catálogo visual de componentes Hub (design system). */
const HubDesignSystemPage: React.FC = () => {
  return <HubDesignSystemShowcase />;
};

export default HubDesignSystemPage;
