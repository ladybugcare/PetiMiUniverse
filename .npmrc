# Monorepo: overrides fixam React 18 / @types/react 18; react-native pede peerOptional @types/react ^19.
# Sem legacy-peer-deps, npm 9+ falha no resolve; com isso, o lock fica consistente com npm ci (Railway).
legacy-peer-deps=true
